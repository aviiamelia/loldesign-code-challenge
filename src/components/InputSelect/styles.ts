import styled from "styled-components";

export const Select = styled.select`
  border: 3px solid var(--red);
  width: 200px;
  height: 45px;
  border-radius: 15px;
  cursor: pointer;
  margin-bottom: 5px;
`;
